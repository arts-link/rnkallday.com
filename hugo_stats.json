# Check for Hugo config files
ls -la config* *.toml *.yaml *.yml 2>/dev/null || echo "No config files found at root"

# Check themes directory structure
find themes -type f -name "*.html" | head -20

# Check the problematic index.html file
head -200 themes/portfolio/layouts/index.html | tail -60
